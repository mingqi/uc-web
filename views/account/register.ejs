<!DOCTYPE html>
<html>
<head>
    <title>注册 - UCLogs</title>
    {% include ../include/inheader %}
    <link rel="stylesheet" href="/css/compiled/sign-up.css" type="text/css" media="screen" />
</head>
<body>
    
    <div class="navbar navbar-inverse navbar-static-top" role="navigation">
    {% include ../include/navbar %}
    </div>

    <div id="sign_up2">
        <div class="container">
            <div class="section_header text-center">
                <h3>注册</h3>
            </div>
            <div class="row login">
                <div class="col-sm-6 col-sm-offset-3 signin_box">
                    <div class="box">
                        <div class="box_cont">

                            <div class="form">
                                <form method="post" action="/account/postRegister">
                                    <input type="hidden" name="rd" value="{%- rd %}"/>
                                    {% if (error) { %}
                                        <div class="alert alert-danger">{%= error %}</div>
                                    {% } %}
                                    <input type="text" name="email" placeholder="Email" value="{%- user && user.email %}" class="form-control">
                                    <input type="password" name="password" placeholder="密码" value="{%- user && user.password %}" class="form-control">
                                    <input type="password" name="password2" placeholder="重复密码" class="form-control">
                                    {% if (needInviteCode) { %}
                                        <input type="text" value="{%- inviteCode %}" name="inviteCode" placeholder="邀请码" class="form-control">
                                    {% } %}
                                    <div class="forgot">
                                        <span>已经注册过?</span>
                                        <a href="/account/login">登录</a>
                                    </div>
                                    <input type="submit" value="注册">
                                </form>
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div> 
    
    
    {% include ../include/footer %}
</body>
</html>